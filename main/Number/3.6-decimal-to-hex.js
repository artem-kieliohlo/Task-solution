// Преобразование десятичных значений
// в шестнадцатеричные
// Задача
// Есть десятичное число, нужно вычислить его шестнадцатеричный эквивалент.

{
  let decimalNumber = 12854;
  // eslint-disable-next-line no-unused-vars
  let hexString = decimalNumber.toString(16);
}
