// Извлечение из массива элементов, удовлетворяющих заданному условию
// Задача
// Найти все элементы массива, удовлетворяющие некоторому условию, и скопи-
// ровать их в новый массив.
{
  let arr = [1, 32, 53, 643, 23];
  let result = arr.filter((value) => value > 25);
}
