// Копирование части массива, выбранной по положению элемента
// Задача
// Скопировать часть массива, не изменив исходный массив.
{
  const sourceArray = [1, 2, 4, 5, 7, 23,34];
  const slicedPart = sourceArray.slice(3, 6);
  console.log(slicedPart);
}
